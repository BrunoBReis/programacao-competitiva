#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll solve(const string &s) {
  return;
}

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(NULL);

  string s;
  cin >> s;

  cout << solve(s) << '\n';

  return 0;
}