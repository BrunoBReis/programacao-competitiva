#include <bits/stdc++.h>

using namespace std;

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(NULL);

  int N;
  cin >> N;
  pair<string, string> a;
  while (N--) {
    cin >> a.first >> a.second;
    
  }
  
  return 0;
}