#include <bits/stdc++.h>

using namespace std;

int main(void) {
  ios::sync_with_stdio(false);
  cin.tie(NULL);

  int A, P, T, M;
  cin >> A >> P >> T >> M;
  cout << "Total de eleitores: " << A + P + T + M << '\n';

  return 0;
}